<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission  android:name="android.permission.SEND_SMS"/>
    <uses-feature android:name="android.hardware.telephony" android:required="true"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eventtracker_TyO"
        >

        <!-- MainActivity: Login screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- EventActivity: Event tracking screen -->
        <activity
            android:name=".EventActivity"
            android:label="Event Tracker"
            android:theme="@style/Theme.AppCompat"/>

        <!-- PermissionsActivity: Permissions screen -->
        <activity
            android:name=".PermissionsActivity"
            android:label="Permissions"
            android:theme="@style/Theme.AppCompat"/>

        <!-- NotificationsSettingsActivity: Settings screen -->
        <activity
            android:name=".NotificationSettingsActivity"
            android:label="Settings"
            android:theme="@style/Theme.AppCompat"/>

        <!-- NotificationReceiver: Notification broadcast receiver -->
        <receiver android:name=".NotificationReceiver"
            android:exported="false"/>

    </application>
</manifest>
